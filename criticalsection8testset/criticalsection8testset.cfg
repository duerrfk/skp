SPECIFICATION Spec
CONSTANT defaultInitValue = defaultInitValue
\* Add statements after this line.

\* The number of concurrent processes trying to enter the critical section
N = 3

\* The implementation guarantees mutual exclusion
PROPERTY MutualExclusion

\* The implementation is deadlock free
PROPERTY NoDeadlock

\* The implementation is *not* starvation free
PROPERTY NoStarvation
